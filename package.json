{
  "name": "javi",
  "version": "0.1.0",
  "description": "Javi — local voice AI orchestrator: reactive particle orb UI wired to a local Ollama model",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^8.1.0",
    "vitest": "^4.1.9"
  }
}
